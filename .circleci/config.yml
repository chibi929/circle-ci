version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8.9.1
    steps:
      - checkout
      - run:
          name: System Information
          command: |
            pwd
            echo "node: $(node -v)"
            echo "npm: $(npm -v)"
            echo "yarn $(yarn -v)"
        
